import { keyframes } from 'styled-components';

const ResultFadeIn = keyframes`
  to {
    opacity: 1;
        visibility: visible;
        top: 0;
        margin-left: 500px;
  }
`;

export default ResultFadeIn;
